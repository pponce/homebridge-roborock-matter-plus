diff --git a/src/hap_schedule_accessory.ts b/src/hap_schedule_accessory.ts
index 47771a3..FINALFIX1 100644
--- a/src/hap_schedule_accessory.ts
+++ b/src/hap_schedule_accessory.ts
@@ -96,6 +96,8 @@ export default class RoborockHapScheduleAccessory {
   private lastScheduleRefreshAt = 0;
   private lastFailedRefreshAt = 0;
   private refreshInProgress: Promise<RoborockScheduleRefreshResult> | undefined;
+  private refreshStartedAt = 0;
+  private lastRefreshStartedAt = 0;
 
   constructor(
@@ -140,7 +142,7 @@ export default class RoborockHapScheduleAccessory {
     info.setCharacteristic(
       this.platform.Characteristic.Model,
-      "Roborock Schedules"
+      "Schedules"
     );
@@ -184,7 +186,7 @@ export default class RoborockHapScheduleAccessory {
       now - this.lastFailedRefreshAt < SCHEDULE_FAILURE_BACKOFF_MS
     ) {
-      this.platform.log.info(
+      this.platform.log.debug(
         `Schedule refreshIfNeeded: FAILURE BACKOFF; ` +
           `ageMs=${now - this.lastFailedRefreshAt}; ` +
           `returning=${(this.cachedSchedules?.length ?? 0) > 0}`
@@ -201,9 +203,10 @@ export default class RoborockHapScheduleAccessory {
 
   async refreshAndGetSchedule(
-    scheduleId: string
+    scheduleId: string,
+    minimumRefreshStartedAt = 0
   ): Promise<RoborockSchedule | undefined> {
-    await this.refreshDetailed();
+    await this.refreshDetailed(minimumRefreshStartedAt);
 
     const schedule = this.cachedSchedules?.find(
@@ -218,20 +221,42 @@ export default class RoborockHapScheduleAccessory {
     return schedule
       ? {
@@ -243,21 +268,30 @@ export default class RoborockHapScheduleAccessory {
     }
 
-  private async refreshDetailed(): Promise<RoborockScheduleRefreshResult> {
+  private async refreshDetailed(
+    minimumRefreshStartedAt = 0
+  ): Promise<RoborockScheduleRefreshResult> {
     if (this.disposed) {
       return {
@@ -268,15 +302,28 @@ export default class RoborockHapScheduleAccessory {
       };
     }
 
-    if (this.refreshInProgress) {
+    if (this.refreshInProgress && this.refreshStartedAt >= minimumRefreshStartedAt) {
       return this.refreshInProgress;
     }
 
-    this.refreshInProgress = this.performRefresh();
+    const refreshStartedAt = Date.now();
+    this.refreshStartedAt = refreshStartedAt;
+    this.lastRefreshStartedAt = refreshStartedAt;
+
+    const refreshPromise = this.performRefresh(refreshStartedAt);
+    this.refreshInProgress = refreshPromise;
     try {
-      return await this.refreshInProgress;
+      return await refreshPromise;
     } finally {
-      this.refreshInProgress = undefined;
+      if (this.refreshInProgress === refreshPromise) {
+        this.refreshInProgress = undefined;
+      }
     }
   }
 
-  private async performRefresh(): Promise<RoborockScheduleRefreshResult> {
+  private async performRefresh(
+    refreshStartedAt: number
+  ): Promise<RoborockScheduleRefreshResult> {
     try {
       const api = this.platform.roborockAPI as any;
@@ -294,6 +341,13 @@ export default class RoborockHapScheduleAccessory {
         }
       );
 
+      if (refreshStartedAt !== this.lastRefreshStartedAt) {
+        return {
+          success: false,
+          hasSchedules: this.scheduleAccessories.size > 0,
+        };
+      }
+
       if (!Array.isArray(raw)) {
         this.lastFailedRefreshAt = Date.now();
@@ -418,6 +472,35 @@ export default class RoborockHapScheduleAccessory {
     }
   }
 
+  private restoreExistingScheduleServices(): boolean {
+    const restored: RoborockSchedule[] = [];
+
+    for (const service of this.managerAccessory.services) {
+      if (service.UUID !== this.platform.Service.Switch.UUID) {
+        continue;
+      }
+
+      const subtype = (service as { subtype?: string }).subtype;
+      if (!subtype?.startsWith(SERVICE_PREFIX)) {
+        continue;
+      }
+
+      let scheduleId: string;
+      try {
+        scheduleId = decodeURIComponent(subtype.slice(SERVICE_PREFIX.length));
+      } catch {
+        continue;
+      }
+
+      if (!scheduleId) {
+        continue;
+      }
+
+      const enabled = Boolean(
+        service.getCharacteristic(this.platform.Characteristic.On).value
+      );
+
+      restored.push({
+        id: scheduleId,
+        enabled,
+        timer: [scheduleId, enabled ? "on" : "off"],
+      });
+    }
+
+    if (restored.length === 0) {
+      return false;
+    }
+
+    restored.sort((a, b) =>
+      a.id.localeCompare(b.id, undefined, { numeric: true })
+    );
+    this.sync(restored);
+    return this.scheduleAccessories.size > 0;
+  }
+
   recordScheduleUpdate(schedule: RoborockSchedule): void {
@@ -564,10 +647,15 @@ class RoborockHapScheduleSwitchAccessory {
-      await updateServerTimer(api, this.duid, this.scheduleId, enabled, {
+      const writeStartedAt = Date.now();
+
+      await updateServerTimer(api, this.duid, this.scheduleId, enabled, {
         requestTimeoutMs: 10000,
       });
 
-      if (!(await this.verify(api, enabled))) {
+      if (!(await this.verify(enabled, writeStartedAt))) {
         this.platform.log.warn(
           `Roborock schedule ${this.scheduleId} did not reflect upd_server_timer; trying upd_timer fallback.`
         );
 
+        const fallbackWriteStartedAt = Date.now();
         await updateTimer(api, this.duid, this.scheduleId, enabled, {
           requestTimeoutMs: 10000,
         });
 
-        if (!(await this.verify(api, enabled))) {
+        if (!(await this.verify(enabled, fallbackWriteStartedAt))) {
           throw new Error(
             `Roborock did not confirm schedule ${this.scheduleId} as ${enabled ? "enabled" : "disabled"}`
           );
@@ -607,13 +695,16 @@ class RoborockHapScheduleSwitchAccessory {
-  private async verify(_api: any, enabled: boolean): Promise<boolean> {
+  private async verify(
+    enabled: boolean,
+    writeStartedAt: number
+  ): Promise<boolean> {
     await new Promise<void>((resolve) => {
       const timer = scheduleTimer(resolve, VERIFY_DELAY_MS);
       unrefTimer(timer);
     });
 
     const current = await this.coordinator.refreshAndGetSchedule(
-      this.scheduleId
+      this.scheduleId,
+      writeStartedAt
     );
 
     if (!current) {
       return false;
*** Update File: src/hap_schedule_accessory.ts
@@
   async initialize(vacuumName: string): Promise<RoborockScheduleRefreshResult> {
@@
-    // Initial discovery always performs one cloud schedule request.
-    // Subsequent HomeKit reads use the cached snapshot until it expires.
-    return this.refreshDetailed();
+    // Initial discovery always performs one cloud schedule request.
+    // Subsequent HomeKit reads use the cached snapshot until it expires.
+    const result = await this.refreshDetailed();
+
+    // Homebridge may restore the manager accessory and its Switch services
+    // from disk before this coordinator is reconstructed. If the first cloud
+    // refresh fails, reattach the handlers to those restored services instead
+    // of unregistering them or leaving them served only by stored HAP state.
+    if (!result.success && this.scheduleAccessories.size === 0) {
+      return {
+        ...result,
+        hasSchedules: this.restoreExistingScheduleServices(),
+      };
+    }
+
+    return result;
   }
*** Update File: src/platform.ts
@@
       if (schedule) {
         void schedule
           .initialize(target.vacuumName)
           .then((result) => {
-            if (result.success && result.hasSchedules) {
+            if (!result.success || result.hasSchedules) {
               return;
             }
@@
             this.removeHapScheduleAccessory(duid, accessory);
           })
           .catch((error: unknown) => {
-            const accessory = this.accessories.find(
-              (candidate) =>
-                candidate.UUID ===
-                  this.api.hap.uuid.generate(
-                    `hap:roborock:schedules:${duid}`
-                  ) && isHapScheduleAccessory(candidate)
-            );
-
-            this.removeHapScheduleAccessory(duid, accessory);
-
             this.log.debug(
               `Unable to refresh Roborock schedules for ${target.vacuumName}: ${
                 error instanceof Error ? error.message : String(error)
@@
       void schedule
         .initialize(target.vacuumName)
         .then((result) => {
           if (!result.success) {
-            this.hapScheduleAccessories.delete(duid);
             return;
           }
@@
         })
         .catch((error: unknown) => {
-          // A first-time offline/error response must not create a broken
-          // "Not Supported" schedule tile.
-          this.removeHapScheduleAccessory(duid, accessory);
-
           this.log.error(
             `Unable to initialize Roborock schedules for ${target.vacuumName}: ${
               error instanceof Error ? error.message : String(error)
*** Update File: __tests__/hap-schedule-cache.test.js
@@
   coordinator.lastFailedRefreshAt = 0;
   coordinator.refreshInProgress = undefined;
+  coordinator.refreshStartedAt = 0;
+  coordinator.lastRefreshStartedAt = 0;
   coordinator.sync = jest.fn();
@@
   test("refreshAndGetSchedule coalesces with an in-flight refresh", async () => {
@@
     expect(getServerTimers).toHaveBeenCalledTimes(1);
   });
+
+  test("verification does not join a refresh that started before the write", async () => {
+    const coordinator = makeCoordinator();
+
+    let resolveOldRefresh;
+    let resolveNewRefresh;
+
+    getServerTimers
+      .mockReturnValueOnce(
+        new Promise((resolve) => {
+          resolveOldRefresh = resolve;
+        })
+      )
+      .mockReturnValueOnce(
+        new Promise((resolve) => {
+          resolveNewRefresh = resolve;
+        })
+      );
+
+    const oldRefresh = coordinator.refresh();
+    await Promise.resolve();
+
+    const writeStartedAt = coordinator.refreshStartedAt + 1;
+    const verification = coordinator.refreshAndGetSchedule(
+      "timer-1",
+      writeStartedAt
+    );
+
+    expect(getServerTimers).toHaveBeenCalledTimes(2);
+
+    resolveNewRefresh([["timer-1", "off"]]);
+
+    await expect(verification).resolves.toEqual({
+      id: "timer-1",
+      enabled: false,
+      timer: ["timer-1", "off"],
+    });
+
+    // The pre-write request finishing later must not overwrite the newer
+    // post-write snapshot.
+    resolveOldRefresh([["timer-1", "on"]]);
+    await oldRefresh;
+
+    expect(coordinator.cachedSchedules).toEqual([
+      {
+        id: "timer-1",
+        enabled: false,
+        timer: ["timer-1", "off"],
+      },
+    ]);
+  });
*** Update File: __tests__/schedule-settings-contract.test.js
@@
-  test("restored schedule groups are removed when initial refresh fails", () => {
+  test("restored schedule groups are preserved when initial refresh fails", () => {
     expect(platformSource).toMatch(
-      /if \(schedule\) \{[\s\S]*?\.initialize\(target\.vacuumName\)[\s\S]*?if \(result\.success && result\.hasSchedules\) \{[\s\S]*?return;[\s\S]*?this\.removeHapScheduleAccessory\(duid, accessory\);/
+      /if \(schedule\) \{[\s\S]*?\.initialize\(target\.vacuumName\)[\s\S]*?if \(!result\.success \|\| result\.hasSchedules\) \{[\s\S]*?return;[\s\S]*?this\.removeHapScheduleAccessory\(duid, accessory\);/
     );
   });
@@
-  test("failed restored schedule discovery unregisters the cached accessory", () => {
+  test("failed restored schedule discovery does not unregister the cached accessory", () => {
     expect(platformSource).toMatch(
-      /if \(schedule\) \{[\s\S]*?if \(result\.success && result\.hasSchedules\) \{[\s\S]*?this\.removeHapScheduleAccessory\(duid, accessory\);/
+      /if \(schedule\) \{[\s\S]*?if \(!result\.success \|\| result\.hasSchedules\) \{[\s\S]*?this\.removeHapScheduleAccessory\(duid, accessory\);/
     );
+
+    expect(platformSource).not.toMatch(
+      /\.catch\(\(error: unknown\) => \{[\s\S]*?removeHapScheduleAccessory\(duid, accessory\);/
+    );
 
     expect(platformSource).toMatch(
       /private removeHapScheduleAccessory\([\s\S]*?unregisterPlatformAccessories/
@@
   test("routine schedule cache decisions are debug-level", () => {
@@
-    // Failure backoff is intentionally an info-level operational signal.
-    expect(refreshBlock).toContain("this.platform.log.info(");
+    expect(refreshBlock).not.toContain("this.platform.log.info(");
+    expect(refreshBlock).toContain("this.platform.log.debug(");
     expect(refreshBlock).toContain(
       "`Schedule refreshIfNeeded: FAILURE BACKOFF; `"
     );
@@
   test("schedule verification refreshes through the coordinator", () => {
@@
     expect(scheduleSource).toContain("return current.enabled === enabled;");
+    expect(scheduleSource).toContain("minimumRefreshStartedAt");
+    expect(scheduleSource).toContain("writeStartedAt");
   });
+
+  test("schedule verification no longer accepts an unused API parameter", () => {
+    expect(scheduleSource).not.toContain("verify(_api, enabled)");
+  });
+
+  test("schedule accessory model follows the parent convention", () => {
+    expect(scheduleSource).toContain('this.platform.Characteristic.Model,\n      "Schedules"');
+    expect(scheduleSource).not.toContain('this.platform.Characteristic.Model,\n      "Roborock Schedules"');
+  });
+
+  test("failed initial discovery reattaches handlers to restored services", () => {
+    expect(scheduleSource).toContain("restoreExistingScheduleServices");
+    expect(scheduleSource).toContain("this.sync(restored);");
+    expect(scheduleSource).toContain("service.getCharacteristic(this.platform.Characteristic.On)");
+  });
*** End Patch
